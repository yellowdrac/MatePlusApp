using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class FinalUIController : MonoBehaviour
{
    [SerializeField] private TextMeshProUGUI txtStatisticsPrt1;
    [SerializeField] private TextMeshProUGUI txtStatisticsPrt2;
    [SerializeField] private TextMeshProUGUI txtStatisticsPrt3;
    [SerializeField] private TextMeshProUGUI txtStatisticsPrt4;
    
    // Start is called before the first frame update
    void Start()
    {
        // txtStatisticsPrt1.text = PlayerLevelInfo.currentLevel.ToString();
        // txtStatisticsPrt1.text = PlayerLevelInfo.currentLevel.ToString();
        // txtStatisticsPrt1.text = PlayerLevelInfo.currentLevel.ToString();
        // txtStatisticsPrt1.text = PlayerLevelInfo.currentLevel.ToString();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
